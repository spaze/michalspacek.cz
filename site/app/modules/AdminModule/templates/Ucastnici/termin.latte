{define #content}
<ol n:inner-foreach="$applications as $application">
	<li>{$application->name} ({$application->status}) <a n:if="$application->hasFiles" n:href="Ucastnici:soubory $application->id">soubory</a></li>
</ol>

{/define}
